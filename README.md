# Getting Started

* clone the repo with `$ git clone https://github.com/charlie-carpene/THP_Next_06-semaine_social-network.git`
* Go to folder
* Run `$ npm install`
* Then run `$ npm start`

*nb : change page to update states if the SPA doesn't update itself. redirections are not set yet*
